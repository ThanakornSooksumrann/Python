ทำการติดตั้ง 
pip install Flask
pip install gensim
pip install nltk
pip install itertools

ในไฟล์ upload.py ต้องมีการแก้ไขโค้ดเมื่อต้องเริ่ม Run บนเครื่องใหม่ แก้ไขตรง Path ในการ เรียกไฟล์และเก็บไฟล์
โดยในโค้ดจะมี comment นี้บอกอยู่ให้เข้าไปเปลี่ยนทุกจุด
#****Path ของเครื่องตัวเอง C:/Users/ชื่อเครื่องตัวเอง/Desktop/Web-NLP/uploads****

และถ้าจะให้ดีให้วาง folder ที่แตกไฟล์นี้ไว้ที่หน้า Desktop เพราะจะได้แก้ไข path แค่ชื่อ user ของคอมเครื่องนั้น ๆ 
